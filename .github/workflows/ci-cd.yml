name: ci-cd

on:
  push:
    branches: [ main ]
  pull_request:

env:
  AWS_REGION: eu-central-1
  ECR_REPOSITORY: whatshouldido-api
  CLUSTER_NAME: whatshouldido-cluster
  NAMESPACE: whatshouldido
  DOTNET_VERSION: '9.0.x'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore
        run: dotnet restore ./API/WhatShouldIDo.sln

      - name: Build
        run: dotnet build ./API/WhatShouldIDo.sln -c Release --no-restore

  docker_push_deploy:
    needs: build
    runs-on: ubuntu-latest
    continue-on-error: true  # Don't fail the workflow if deployment is not configured yet
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        continue-on-error: true
        with:
          role-to-assume: arn:aws:iam::338630859894:role/GitHubOIDCRole
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to ECR
        uses: aws-actions/amazon-ecr-login@v2
        continue-on-error: true

      - name: Build & Push Image
        continue-on-error: true
        run: |
          IMAGE_TAG=${{ github.sha }}
          REPO_URI=$(aws ecr describe-repositories --repository-names $ECR_REPOSITORY --query 'repositories[0].repositoryUri' --output text)
          docker build -t $REPO_URI:$IMAGE_TAG -f ./API/src/WhatShouldIDo.API/Dockerfile ./API
          docker push $REPO_URI:$IMAGE_TAG
          echo "IMAGE_URI=$REPO_URI:$IMAGE_TAG" >> $GITHUB_ENV

      - name: Update Kubeconfig
        continue-on-error: true
        run: aws eks update-kubeconfig --name $CLUSTER_NAME --region $AWS_REGION

      - name: Rollout to EKS
        continue-on-error: true
        run: |
          kubectl -n $NAMESPACE set image deployment/whatshouldido-api whatshouldido-api=$IMAGE_URI
          kubectl -n $NAMESPACE rollout status deployment/whatshouldido-api
